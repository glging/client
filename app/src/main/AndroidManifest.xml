<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dku.gyeongsotone.gulging.campusplogging">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusPlogging">
        <activity android:name=".ui.main.MainActivity"></activity>
        <!-- 로그인/회원가입 액티비티 -->
        <activity android:name=".ui.auth.AuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 재학 인증 액티비티 -->
        <activity android:name=".ui.univcertification.UnivCertificationActivity" />
    </application>

</manifest>